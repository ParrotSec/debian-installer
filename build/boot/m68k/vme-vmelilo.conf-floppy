prompt  = true
default = install
message = /debian.txt
root    = /dev/sda
image   = /linux
read-write

[boot]
label   = Install
append  = load_ramdisk=1 prompt_ramdisk=1

[boot]
label   = Rescue
append  = load_ramdisk=1 prompt_ramdisk=1 single

