#include "base"
#include "kernel"

# Other udebs that are needed for CDROM install
installation-locale
localechooser
hw-detect
cdrom-detect
cdrom-retriever
load-cdrom
cdrom-checker
bogl-bterm-udeb
di-utils-terminfo
cdebconf-newt-udeb
cdebconf-newt-terminal ?
cdebconf-text-udeb
brltty-udeb
preseed-common
initrd-preseed
file-preseed

nano-udeb

# This is so additional modules (eg, scsi) can be loaded from driver
# floppies. cdrom-detect will use it.
media-retriever

save-logs

# for saving logs, and loading drivers from media
mountmedia

libfribidi0-udeb
